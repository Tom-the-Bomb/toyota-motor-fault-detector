# Motor Fault Detection - ML Model report

- Trained an **LSTM** model with sampled data from a DC motor connected to an arduino
  - motor represents an actual motor in the Toyota factory
  - arduino uses UART to feed current (A) data for sampling
